import { Component, ViewChild, Input, Output, ElementRef, EventEmitter } from '@angular/core';

@Component({
    selector: 'rodape',
    templateUrl: './rodape.html',
    providers: [],
    styleUrls: ['./rodape.scss'],
})
export class Rodape {
}